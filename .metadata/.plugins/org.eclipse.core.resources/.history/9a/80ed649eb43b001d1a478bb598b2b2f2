import junit.framework.TestCase;

public class FormularTest extends TestCase {
public void testAreaTriangle() {
	try {
		FormularUtil.areaTriangle(4, 2, 1);
	} catch (Exception e) {
		assertEquals(e.getMessage(), "It's not triangle");
	}
}
}
