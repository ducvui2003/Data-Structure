package exerceses;

public class Ex5 {
	public static int totalNumber (String[]arrayNumber, int length) {
		length--;
		if (length ==0) {
			return Integer.parseInt(arrayNumber[length]);
		}else {
			return arrayNumber[length] + totalNumber(arrayNumber, length);
		}
	}
}
